spring:
  datasource:
    url: jdbc:mysql://localhost:3308/invoice_database
    username: admin
    password: admin-gespyme-2024-99Pabcde
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8084

